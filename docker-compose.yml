services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db

  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB=mongodb://mongo:27017/voip
      - PORT=8080
    depends_on:
      - mongo

volumes:
  mongo_data:
